# v3 changes

- Restored all 10 original skill atoms as active runtime-specific skill folders.
- Added runtime-specific target/control skill names while preserving original source under `original-skills/`.
- Added duplicate active skill locations required/expected by Pi Agent CLI Control.
- Added validators that fail if any converted skill folder is missing.
- Kept full `tctl`, scripts, diagrams, and fixed Remotion showcase project.
